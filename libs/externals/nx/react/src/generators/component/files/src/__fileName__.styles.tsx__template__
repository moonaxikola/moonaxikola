import { styled } from '@mui/material/styles';

import type { <%= fileName %>Props } from './<%= fileName %>.types';

export const Root = styled('div')<<%= fileName %>Props>(({ theme }) => ({
  position: 'inherit',
}));
