import type { <%= fileName %>Props } from './<%= fileName %>.types';
import { Root } from './<%= fileName %>.styles';

export function <%= fileName %>({ children }: <%= fileName %>Props) {
  return (
    <Root>
      {children}
    </Root>
  );
}
